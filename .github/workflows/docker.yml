name: Docker

on:
  - push
  - repository_dispatch

jobs:
  build-images:
    strategy:
      matrix:
        arch:
          - amd64
          - arm32v5
          - arm32v7
          - arm64v8
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Build image
        env:
          DOCKER_BUILDKIT: 1
        run: ./build --prefix masnagam --no-cache --squash ${{ matrix.arch }}
      - name: Login to DockerHub
        run: docker login -u masnagam -p ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Push image
        run: docker push masnagam/mirakc:${{ matrix.arch }}
  build-multiarch-image:
    runs-on: ubuntu-latest
    needs: build-images
    steps:
      - name: Set up Docker Buildx
        id: buildx
        uses: crazy-max/ghaction-docker-buildx@v1
        with:
          version: latest
      - uses: actions/checkout@v1
      - name: Login to DockerHub
        run: docker login -u masnagam -p ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push a multiarch image to DockerHub
        run: >-
          docker buildx build
          -f Dockerfile.multiarch
          -t masnagam/mirakc
          --platform 'linux/amd64,linux/arm/v5,linux/arm/v7,linux/arm64/v8'
          --push
          .
